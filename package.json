{
  "scripts": {
    "transpile": "ngc",
    "package": "rollup -c",
    "minify": "uglifyjs dist/bundles/angular-ab-tests.umd.js --screw-ie8 --compress --mangle --comments --output dist/bundles/angular-ab-tests.umd.min.js",
    "build": "npm run transpile && npm run package && npm run minify"
  }
}
